<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>All Users</title>
  </head>
  <body>
    <% User.all.each do |user| %>
      <h1> Post-iter: <a href="/users/<%= user.id %>"><%= user.name %> </a> </h1>
    <% end %>
  </body>
</html>
